{
  "name": "winechord-books",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs --host 127.0.0.1",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs --host 127.0.0.1",
    "check:content": "node scripts/check-content.mjs",
    "check:dist": "bash scripts/check-dist.sh",
    "verify": "npm run check:content && npm run build && npm run check:dist"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
